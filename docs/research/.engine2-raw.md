[Engine 2 / codex, live web search. Full result archived from task ac838228324183aef]
See report body; key claims merged into claim table. Distinct contributions vs engine 1:
- DefectDojo counts: 132 HASHCODE_FIELDS_PER_SCANNER entries, 193 DEDUPLICATION_ALGORITHM_PER_PARSER entries (configured keys, not distinct parser classes).
- DefectDojo hash routine: unframed concatenation (NO separators) -> boundary ambiguity; casefold; SHA-256. DO NOT copy the concatenation; use canonical sorted-key JSON + framed digest.
- DefectDojo source comments document per-scanner REPAIRS for ffuf/Dirsearch/Gobuster/WhatWeb/Naabu/Masscan/sqlmap/Nettacker/httpx because their descriptions carried changing sizes/versions/timestamps/random payloads -> moved to title+endpoint. Primary-source proof the contamination problem is universal and solved per-scanner.
- SARIF baselineState = new/unchanged/updated/absent; "comprehensive comparison" rule: if one result carries baselineState all must -> a partial/failed scan must not emit authoritative fixed rows.
- Trivy Fingerprint = SHA256(ArtifactID:Target:PkgID:VulnerabilityID); PkgID includes VERSION -> tracks exact-artifact observation, not logical vuln across patched versions. Exclude installed version from identity.
- New OSS refs: SecObserve (BSD-3, Python, import lifecycle indexing by identity hash - closest lifecycle ref); Semgrep rule_match.py (Python code-finding key); microsoft/sarif-tools (MIT pip, diff skips equal-count appear/disappear); github/codeql-action fingerprints.ts (content-relative line hash); trivy-plugin-compare (CVE-id sets only - UNSAFE); GitLab dedup (architectural ref).
- Verdict: no Python package safely solves identity for 41 heterogeneous tools; build a small local typed fingerprint registry, per-scanner validated before it may emit FIXED.
- Confidence tiers: A/B may close baseline; C "probable unchanged" must NOT close; D advisory/manual. Never use list position as identity or tie-breaker.
